#!groovy

@Library("Infrastructure")

def type = "java"
def product = "civil"
def component = "commons"

withPactTestOnlyPipeline(type, product, component) {
    enablePactAs([
            AppPipelineDsl.PactRoles.CONSUMER
    ])
}
